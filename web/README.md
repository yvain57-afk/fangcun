# 方寸 Fangcun · 高保真交互测试原型

依据用户提供的《全套 UI 改造深化设计系统与完整规格》制作：方案 A 轻版画日常，融合方案 B 猫狗情境姿态。交付日期：2026-09-17。

## 打开预览

当前机器预览：<http://127.0.0.1:4173/>。

猫狗形态与动效工作台：<http://127.0.0.1:4173/character-studio.html>。可分别重播、暂停、查看单独与双角色原画，以及切换深浅背景。

双击 **启动预览.command**。已运行时直接打开，未运行时启动本地服务。关闭启动终端将停止服务。需要 Node.js 22.12 或更新版本；首次启动会通过 npm 安装锁定版本的依赖，之后可离线运行。

也可以在项目目录执行：

```sh
npm ci --no-audit --no-fund
npm run dev
```

这是本机 Web 原型，地址仅在运行它的电脑上有效。手机尺寸预览已内置；尚未部署公网或安装进 iPhone。

## 已实现的测试链路

- **今日**：平稳 / 偏高 / 数据不足三种演示情景；清晰结论、依据摘要、一键进入 5 分钟练习；依据详情。
- **陪伴角色**：以用户确认稿为参考，通过 imagegen 生成情景原画；本轮保留猫咪，重新设计圆脸垂耳的小狗，并增加单独出场；保留连贯圆润的轮廓、蓝橙双色和版画印痕。原画以局部网格变形呈现轻微动作，首页可点击查看大图；练习页胸腹随节拍起伏，脚底固定。
- **饮品记录**：半屏弹层，水、咖啡（美式 / 甜拿铁）、酒精、糖饮快速增减；固定底部总量、撤销上一笔；甜拿铁同时累计饮品体积、咖啡因和糖饮份数，不重复累计体积。
- **沉浸练习**：300 秒双吸一呼，暂停 / 继续、退出确认、提前结束也记录实际时长；页面隐藏时自动暂停；完成后可选择反馈并返回。
- **趋势**：7 天状态分类柱，点选日期展示快照；数据不足使用空心标记，不当成零压力。示例周固定为 2026-09-11 至 09-17，最后一天联动当前演示场景和当日试用记录。
- **设置**：深浅色、大字号、减少动态效果；咖啡因每杯估值可改，旧记录保留录入时数值。
- **本地保存**：饮品和练习按本机日期存入浏览器 localStorage；不上传。禁用存储或空间不足时显示未保存提示。

## 设计与规格取舍

- 色彩来自文档 Tokens：版画蓝 `#1B355A`、印章橙 `#E07A5F`、暖纸白 `#FAF9F5`，深色使用指定的独立调色板；圆角、字号、44 px 点击区域集中管理。
- 以首条原型请求的 **5 分钟**为统一测试流程，三种状态均可直接开始。完整规格中的其他练习、入门授权、睡眠独立详情、Watch 等不属于本次优先原型范围。
- 文档未明确每段秒数，原型暂用 **吸气 4 秒 → 补吸 1 秒 → 呼气 7 秒**，共 25 轮。80% / 20% 表达为图形相对扩张阶段，不表示真实肺容量或测量结果。正式产品的练习参数仍需单独确定。
- 减少动态效果时猫咪和圆环不缩放，只改变光晕透明度和文字节拍。
- 咖啡因采用 mg，默认每杯 140 mg；酒精以预设约 10 g / 份估算。糖饮只显示份数，不根据饮料名称虚构糖克数。饮品总量表示记录体积，不声称等于净补水量。
- 健康指标及压力结论均为 **Mock 演示数据**，没有接入 HealthKit，也没有实现或验证医学压力判定算法。练习结束不会自动把压力改成平稳。
- 大字号是 Web 字号切换，不能替代原生 Dynamic Type 验收；无原生 Taptic、语音节拍或 Watch 同步。

用户原始规格保存在 `docs/用户提供的完整设计规格.md`；确认稿保存在 `public/reference.png`。原始文档保留原文，其产品主张不代表原型已验证的能力。

## 代码结构

```text
src/
  App.jsx                  页面导航、偏好与本地记录
  tokens.css               深浅色、字号、圆角 Tokens
  styles.css               手机布局和响应式样式
  models/                  饮品事务、呼吸节拍、演示数据
  hooks/                   呼吸时钟与暂停行为
  components/              原画渲染与局部动效、弹层、饮品记录
  characterStudio.jsx      单独与双角色形态动效工作台
public/characters/v3/      保留的猫咪与上一版原画
public/characters/v4/      新小狗、单独与双角色情景原画
docs/                     原始规格、生成提示词与资产校验清单
  views/                   今日、依据、练习、完成、趋势、设置
tests/
  models.test.js            记录与节拍边界测试
  ui/prototype.spec.js      浏览器完整链路测试
artifacts/                 浅色、深色大字号、饮品和练习截图
```

技术栈：React、Vite、Tailwind CSS、Framer Motion、Lucide。无需账号、后端或外部图片服务。

## 验证与试用

```sh
npm test
npm run test:ui
npm run build
```

交付时 9 项模型测试、10 项 Chrome 浏览器交互测试通过，生产构建通过。浏览器测试使用本机 Google Chrome；覆盖情景切换、弹层高度、增减撤销、刷新保存、呼吸阶段、暂停退出、300 秒完成、375 px 布局、深色、大字号、缺失数据、减少动态效果。完整 300 秒结束测试使用虚拟时钟加速，不代表已完成真实用户的 5 分钟体验研究。

建议先做三个真实体验任务：

1. 首页只看 3 秒后移开视线，说出今天结论、一个依据和下一步行动。
2. 记录一杯水和一杯甜拿铁，撤销拿铁，观察汇总是否符合预期。
3. 实际练习一轮，再测试暂停与退出，最后比较深色及大字号。

自动测试确认核心信息与入口可见，**3 秒认知效率和舒适度仍需由真实使用者验证**。


## 本轮：保留猫咪，重做小狗，增加单独出场

猫咪的呼吸原画原文件复用。小狗重新设计为圆脸、短身体、两只自然垂下的耳朵；保留白色身体、蓝色版画线条和单侧橙耳。现在包含五张单角色画稿（包括一张小狗形象展示）和两张双角色画稿。

| 画稿 | 出场位置 | 动作 |
| --- | --- | --- |
| dog-greeting.png | 角色工作台，展示新小狗 | 尾巴偶尔轻摆 |
| cat-curious.png | 首页数据不足 | 猫咪独自探身观察小纸页 |
| breathing.png（v3 原文件） | 练习入口、全屏练习 | 猫咪独自呼吸，胸腹跟随节拍 |
| dog-rest.png | 首页偏高、提前结束 | 小狗独自趴下，身体缓缓起伏 |
| dog-drink.png | 饮品弹层、记录反馈 | 小狗坐在水杯旁，记录后轻点头 |
| duo-calm.png | 首页平稳 | 猫咪与新小狗坐在一起，尾巴轻摆 |
| duo-complete.png | 完整练习结束 | 猫咪与新小狗轻碰爪 |

本轮通过内置 image_gen 生成，原画保存在 `public/characters/v4/`；提示词和生成源路径在 `docs/character-artwork-v4-prompts.json`，资产校验在 `docs/character-artwork-v4-manifest.json`。复制原图后未做像素修改。深色保留白色纸面与原画配色。

场景使用独立的资产与角色类型配置 `src/models/characterAssets.js`，避免“单独出场”的页面误用双角色图。首页查看按钮随当前角色显示“看看它”或“看看它们”，放大预览与首页保持同一场景。单角色使用方形展示区域，双角色使用横向区域。

动作使用原生 WebGL 局部网格变形，没有新增依赖；这是原画上的轻微动作，不是逐帧动画。WebGL 不可用时回退到静态原画，离开屏幕或切换到后台时停止绘制。暂停冻结节拍与局部动作，减少动态效果时保留静态姿态。

主要实现：`src/components/CharacterArtwork.jsx`、`src/components/artworkRenderer.js`、`src/models/characterMotion.js`。旧原画与本次修改前的代码均保留；本次代码备份在 `backups/before-dog-redesign-20260917-170155/`。

验证覆盖单独猫咪、单独小狗及双角色的页面匹配、原画加载、脚底固定、局部变形幅度、呼吸与计时同步、暂停冻结、饮品增减撤销、大小字号与深色布局。截图保存在 `artifacts/`。

## 动作与反馈精修

沿用 v4 猫狗原画和单独 / 双角色分工。小狗轻歪头和摆尾，猫咪观察时小幅探身；点头区域让眼睛、鼻子的相对位置保持稳定。饮品操作后，标题下方显示本次增减或撤销结果，同时触发小狗回应。

连续触发动作时，从当前已绘制的姿态用 160 ms 缓出过渡接入新动作，避免突然回到原位。角色大图增加“再看一次动作”；重播保持按钮焦点。减少动态效果时不显示重播入口。

暂停、减少动态效果、后台、屏幕外，以及被弹层或全屏练习遮住的角色停止帧循环。点头和碰爪做完后停止绘制，下一次操作再唤醒；关闭弹层后，首页角色继续原来的进度。弹层沿用 230 ms 进入时长，改用 transform 与 opacity。

时长与曲线集中在 `src/models/motionTokens.js`。相关验证包含快速连续记录、撤销后汇总、后台角色冻结与恢复、重播焦点、暂停及减少动态效果时不增加绘制次数。当前仍是本机 Web 原型；真实手机上的舒适度需继续试用。

## 品牌标志提案：留一方余地

将名称解释为“在纷乱的日常里，留一小处照顾自己的空间”。两段柔软的弧线围合中央留白，保留开口，以蓝色为主体、少量橙色点缀。已替换首页字框标志、网页图标与练习完成页签名，沿用现有猫狗角色。

访问 `/brand-studio.html` 查看寓意、尺寸、单色、深色及角色搭配。SVG 和原始概念图在 `public/brand/`；完整设计说明和生成提示词见 `docs/方寸-logo-留一方余地.md`。这是一版可在原型中比较的品牌提案。

本轮生产构建通过，首页三情景、五分钟完成及移动端深色大字号共 3 项相关浏览器测试通过；人工检查了首页深浅色搭配。修改前的版本在 `backups/before-brand-mark-20260918-000902/`。

## 品牌应用补充

`/brand-studio.html#applications` 新增暖白与深蓝 App 图标、可重播的启动展示、颜色与留白规范；设置页也统一使用新标志，并链接品牌预览。启动动效仅在预览中按需播放一次，支持减少动态效果及离屏暂停，实际首页继续直接进入。

`public/brand/fangcun-brand-kit-v1.zip` 提供五份 SVG 源文件及两份设计说明。当前为 Web 预览与 SVG 设计素材，尚未接入原生 iOS 图标。完整规范在 `docs/方寸品牌使用规范-v1.md`。

本次生产构建通过，新增 2 项浏览器测试通过：重播结束、深浅色、减少动态效果、素材下载、375 px 无横向溢出，以及从设置页打开品牌说明。相关截图为 `artifacts/brand-applications-desktop.png` 和 `artifacts/brand-applications-mobile.png`。
